<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.spectorh.bloodbank">

    <application
        android:allowBackup="true"
        android:icon="@drawable/bld"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".startUp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".donate" />
        <activity android:name=".inventoryView" />
        <activity android:name=".withdraw" />
        <activity android:name=".withdrawTC" />
        <activity android:name=".home"></activity>
    </application>

</manifest>